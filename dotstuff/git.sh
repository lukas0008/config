#!/usr/bin/bash

git config --global user.name lukas0008
git config --global user.email me@zajac.lu
git config --global gpg.format ssh
git config --global user.signingkey ~/.ssh/id_rsa.pub
git config --global commit.gpgsign true

